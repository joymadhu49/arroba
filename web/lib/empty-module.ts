/**
 * Empty stub. `@wagmi/core`'s optional Tempo connector dynamically imports an
 * optional "accounts" package that is not installed; Turbopack fails the build
 * on the unresolved specifier, so next.config.ts aliases it here. Arroba
 * never uses the Tempo connector.
 */
export {};
